<%= form_for @income_category, remote: true do |f| %>
  <% if @income_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@income_category.errors.count, "error") %> prohibited this income_category from being saved:</h2>

      <ul>
      <% @income_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
